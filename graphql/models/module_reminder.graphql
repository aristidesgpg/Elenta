type ModuleReminder {
    id: ID!
    module: Module

    subject: String
    message: String
    frequency: Int!
    max_reminders: Int!

    created_at: DateTime!
    updated_at: DateTime!
    deleted_at: DateTime!
}

input UpsertModuleReminderInput {
    id: ID

    subject: String
    message: String
    frequency: Int
    max_reminders: Int
}
